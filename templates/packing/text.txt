<div class="row justify-content-center mb-4">
      <div class="col-md-8 mt-5">
        <div class="card shadow-sm border-primary">
          <div class="card-header bg-primary text-white text-center">
            {% if ajout_packing %}
              <h4 class="mb-0">📝 Ajouter de packing</h4>
            {% else %}
              <h4 class="mb-0">📝 Ajouter une nouvelle panne</h4>
            {% endif %}
          </div>
          <div class="card-body">
            {% if messages %}
              {% for message in messages %}
                <div class="alert alert-{{ message.tags }} alert-dismissible fade show text-center" role="alert">
                  {{ message }}
                  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
              {% endfor %}
            {% endif %}
            <form action="" method="post">
              {% csrf_token %}
              {% for field in form %}
                <div class="mb-3">
                  <label for="{{ field.id_for_label }}" class="form-label fw-bold">{{ field.label }}</label>
                  {{ field }}
                  {% if field.errors %}
                    <div class="text-danger small fst-italic">{{ field.errors }}</div>
                  {% endif %}
                </div>
              {% endfor %}
              <div class="d-flex justify-content-between mt-4">
                {% comment %} {% if object_pack %}
                  <a href="{% url 'packing:user-packing-detail' user.username %}" class="btn btn-secondary">⬅️ Retour</a>
                {% elif object_pan %}
                  <a href="{% url 'packing:user-packing-panne-detail' user.username %}" class="btn btn-secondary">⬅️ Retour</a>
                {% else %}
                  {% endcomment %}
                  <a href="{% url 'packing:packing-home' %}" class="btn btn-secondary">⬅️ Retour</a>
                  {% comment %}
                {% endif %} {% endcomment %}

                <button type="submit" class="btn btn-success">💾 Enregistrer</button>
              </div>
            </form>
          </div>
          <div class="card-footer text-muted text-center small">Tous les champs sont requis pour une saisie complète.</div>
        </div>
      </div>
    </div>